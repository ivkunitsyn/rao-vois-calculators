# Калькулятор ставок ВОИС для трансляторов мероприятий

Backend-логика расчёта реализована в `backend/calculator.py` и вызывается через API `/api/events/calc`.
Фронтенд: `/interface/index.html` (раздел `vois_events`).

Логика расчёта и схема:
- `/Users/Acer/Documents/New project/RNK/docs/VOIS_EVENTS_LOGIC.md`
